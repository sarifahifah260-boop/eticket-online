[phases.setup]
nixPkgs = ["php82", "php82Extensions.mbstring", "php82Extensions.openssl", "php82Extensions.pdo", "php82Extensions.pdo_mysql", "composer"]

[start]
cmd = "php artisan serve --host=0.0.0.0 --port=$PORT"
